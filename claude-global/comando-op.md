---
description: Avances y consultas de OpenProject/GitLab desde cualquier proyecto (agente openproject)
---

Lanza el agente `openproject` (herramienta Agent, subagent_type "openproject") con esta petición
del usuario, y transmite fielmente su resultado final (tabla de verificación incluida):

$ARGUMENTS

Si la petición está vacía, pídele al agente un resumen: mis tareas abiertas por proyecto y el
estado del triángulo de los proyectos con GitLab.
